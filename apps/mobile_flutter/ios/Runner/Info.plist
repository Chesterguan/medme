<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>医我</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>mobile_flutter</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>NSCameraUsageDescription</key>
	<string>MedMe 需要使用相机拍摄你的化验单、处方等病历,识别文字后保存到你自己的健康档案。照片只存在本机,不会上传。</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>MedMe 需要访问相册,导入你已经拍好的病历照片。照片只存在本机,不会上传。</string>
	<!-- ⚠️ 这条必须留着,来源是 **file_picker**,不是 ML Kit。
	     `file_picker ^8` 的 iOS 实现依赖 DKImagePickerController,其中 `DKCamera` 有个
	     `DKCameraLocationManager`(给照片打地理标记用),会引用 `CLLocationManager` +
	     `requestWhenInUseAuthorization`。苹果按**符号引用**判定,不看有没有真的调用,
	     所以不声明就是 ITMS-90683。
	     曾经这里有两条(连 `NSLocationAlwaysAndWhenInUse`),删 ML Kit 时被一并删掉 ——
	     当时误判两条都来自 ML Kit 的传递依赖,build 49 因此中了警告。
	     **两条都要留。** 曾经按 build 49 的实测(`requestAlwaysAuthorization` 引用数为 0)
	     只补了 WhenInUse 一条,结果 build 50 的产物里那个符号又出现了 —— 源码一个字没改,
	     只改了本文件和版本号。根因是 `ios/Podfile.lock` **不完整**(只锁了 4 个 pod,
	     `file_picker` 拉进来的 DKImagePickerController 一个都没锁),CI 每次 `pod install`
	     都可能解析到不同版本,于是符号集会变。在锁文件补全之前,**别再按某一次产物的
	     实测去删声明** —— 那个实测只对那一次构建成立。
	     核查方法(别再凭印象):解开 ipa 后
	       strings -a Payload/Runner.app/**Runner** | grep -c requestWhenInUseAuthorization
	     注意可执行文件叫 `Runner`,不叫 `mobile_flutter` —— 扫错文件会得到「零引用」的假结论。 -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>MedMe 不获取你的位置,也不会请求定位权限。这条声明是因为导入文件用的系统相册组件引用了定位接口。</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>MedMe 不获取你的位置,也不会请求定位权限。这条声明是因为导入文件用的系统相册组件引用了定位接口。</string>
	<!-- 只用标准/豁免加密(本机端到端分享用的是标准算法),免每次 TestFlight 问出口合规。 -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- 让 iCloud 容器的 Documents 在系统「文件」App → iCloud 云盘里以「MedMe 医我」
	     可见 —— 用户开启同步后能眼见为实地看到病历文件夹(vault),确认数据进了 iCloud。 -->
	<key>NSUbiquitousContainers</key>
	<dict>
		<key>iCloud.com.medme.mobile</key>
		<dict>
			<key>NSUbiquitousContainerIsDocumentScopePublic</key>
			<true/>
			<key>NSUbiquitousContainerName</key>
			<string>MedMe 医我</string>
			<key>NSUbiquitousContainerSupportedFolderLevels</key>
			<string>Any</string>
		</dict>
	</dict>
	<!-- 认领链接:病人在浏览器里看完医生代拍的病历,点「存进我的 MedMe」时由这条
	     scheme 把 fragment 交给 App(见 lib/claim_link.dart)。用自定义 scheme 而不是
	     Universal Links,是因为后者要求校验文件放在域名根目录,而站点现在是 GitHub
	     项目页,根目录不归我们管。买了自有域名后可以再补 Universal Links。 -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.medme.mobile.claim</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>medme</string>
			</array>
		</dict>
	</array>
	<!-- 让 Flutter 引擎把进来的 URL 直接送进 Dart 的路由回调
	     (didPushRouteInformation),省掉一个深链插件。 -->
	<key>FlutterDeepLinkingEnabled</key>
	<true/>
</dict>
</plist>

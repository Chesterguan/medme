# MedMe 工作清单

> 最后更新：2026-08-05 · 分支 `feat/mobile-ia`
> 排序即优先级。危害 > 承诺 > 体验 > 债。

---

## P0 — 会给出错误临床结论

| # | 事项 | 状态 |
|---|---|---|
| ~~19~~ | ✅ **已修** 混合页 PDF 静默丢数据：第一页有文本层就整篇走文本层分支，后续扫描页永不 OCR，UI 却报「成功识别入库」。用户丢了数据且不知道 | ✅ 已合 |
| ~~38~~ | ✅ **已修根因** `ref[50,10]` 不是「parser 该拒绝」的问题，是区间正则 `(\d+(?:\.\d+)?)\s*[-~]\s*(...)` **无边界且不认逗号**：OCR 把小数点读成逗号时匹配从逗号之后开始，`12,5~20` 静默变成 `[5,20]`。会叫的只是少数，安静的那几种才危险 | ✅ 已合 `f4a20f3` |
| ~~49~~ | ✅ **已修** parser **安静地伪造一个生理上合理的临床值**（四条同族，逐条独立复现过）：结果列不认逗号（`肌钙蛋白 0,08` → `0` 判正常，心梗）、参考区间下限被当成结果（`13血红蛋白 4.00~5.50` → hgb=4.0，致死值）、医院名正则无左边界（`审核者:王涛北京协和医院`）。伪造值**永远合理**，图上看不出异常 | ✅ 已合 `4e3eed6` |
| ~~52~~ | ✅ **已修** 单位换算与参考区间脱钩。产品定的方向：**后端锚规范单位，前端显示回印刷值** —— 用户手里那张纸印的是 `1.2 mg/dL`，给他看 `106.104` 违背「原件永远可达」。`finalize_lab_series` 是全项目唯一决定点；换不全时**参考区间整体留空**，宁可没有也不给错单位的 | ✅ 已合 `f47b49e` |
| 29 | **倾斜照片错行一位**：**两轮均判定不做**，两次都有量化证据（表内局部斜率 0.131 ≈ 全页 0.128，真实近失是 ~2.2px 的简并，分带解决不了）。已加 90 检测框的回归哨兵钉住现状。⚠️ 但 `血常规报告4` 上有**同类错误未被保护**：`3淋巴细胞计数=1.48 ref[0.0,0.5]` 被误标偏低，真区间 1.00~3.30 | 🟡 哨兵已合，残留实例未修 |
| ~~53~~ | ✅ **已修** 「清空所有数据」之后写不进任何东西。修的是**顺序契约**：①松手 →②删盘 →③开箱，开箱永远最后一步，不是在末尾补一次开箱。华为真机验过：清空 → 0 份 → 立刻录 126/79 → 1 份，logcat 无 `No such file or directory` | ✅ 已合 `582b3eb` |
| 30 | ARNI 被错标在冠心病/CKD 泳道，`class` 写着「ACEI/ARB」 | 🟡 等临床裁定（issue #190） |

## P1 — 对外承诺与真实能力不符

| # | 事项 | 状态 |
|---|---|---|
| 40 | 查看器「现规律服用 X 治疗」——`MedSpan.status` 恒为 active | ✅ 已修，未部署 |
| ~~39~~ | ✅ **已上线** 官网首页截图 3 → 7 张（全部 1.6.0 真机 360dp），4+3 两排，五档屏宽实测无横向滚动。E2EE 措辞与时间线四色表一并推了 | ✅ `ef01fa9` |
| 41 | 官网「端到端加密」贴在本地存储上，与自家 `privacy.html:244` 矛盾 | 🔵 并入 39 |
| ~~—~~ | ✅ **已解决** viewer 漂移：权威是**仓库源码**，`deploy-viewer.yml` 在 push 到 main 且改动 `web/hosted-viewer/**` 时自动同步。本地那份手工改动与部署结果内容一致，已丢弃。线上现已带 CSP 修复 | ✅ `d1edee2` |
| ~~—~~ | ✅ **已重拍** 那个 bug 的根因是 PDF 文本层的**康熙部首字形**掉出常用汉字区，任何院名正则都匹配不上（22 份 demo 里 13 份空白、1 份读成医嘱）。修后 21/22 抽出干净完整名 | ✅ `8fb35c4` |

## P2 — 功能缺口

| # | 事项 | 状态 |
|---|---|---|
| ~~43~~ | ✅ **已交付·真机验证** 手动录入「记录」：自测数值 + 笔记。设计定稿（`openmed/MANUAL-ENTRY-DESIGN.md`），零新 Event 变体 | ✅ 已合 |
| ~~42~~ | ✅ **已合** 趋势 tag 换成检验大类（血常规/肝功/肾功…），疾病泳道退回医生视图 | ✅ 已合 |
| 17/21/22 | 多页原件：合成一份、20 页硬截断无提示、`page_count` 恒为 1 | 🟡 部分并入 19 |
| 28 | doclens 替代 ML Kit 文档扫描（国内安卓无 GMS） | ⚪️ 需真机实测 |
| 33 | 桌面端断层：新时间线点进去落在没改过的 `DocumentView` | ⚪️ 暂停 |

## P3 — 债与待核

| # | 事项 | 状态 |
|---|---|---|
| ~~4~~ | ✅ **已合安全版** `fix/lab-row-recovery-safe`：折行合并保留，行号剥离收紧到「裸对」才生效，堵住原分支那条 `wbc=4.0` 的路。原 `fix/lab-row-recovery` 作废 | ✅ 已合 `7d78d65` |
| 3 | problem_map 同义词：脂肪肝 | 🔵 评审中（出处需逐条核） |
| ~~31~~ | ✅ **已合** classify 误判导致药名被抽成化验序列 | ✅ 已合 |
| 9 | 53 条药物无 ATC，结构上挂不到任何泳道 | 🟡 `0b0e0b5` 出了完整清单，映射本身仍需临床签字 |
| ~~7~~ | ✅ **已合** `dictionary.json` 重复条目 + 两处测试盲点 | ✅ 已合 `093a02a` |
| 6 | 五处渲染各写一遍（架构债） | ⚪️ 暂不动 |
| 23 | ADR 0007 与 build 41 建立在不存在的根因上，需更正 | ⚪️ |
| 34 | 模拟器导入卡 3 分钟 —— 疑为模拟器慢，需真机验证 | ⚪️ |
| 20 | `page_count` 恒为 1，换页符 heuristic 从未生效且零消费者 | 🟡 并入 19 |

---

## 分支

用 `git branch --no-merged feat/mobile-ia` 核过，不是凭记忆列的。

**未合入的只剩 5 条**：

| 分支 | 处置 |
|---|---|
| `fix/disease-synonyms` | ❌ **评审判定不合**：三条阻塞。最硬的一条是回归 —— `2型糖尿病伴酒精性脂肪肝` 会让整条诊断串返回 `None`，病人的糖尿病泳道连同化验用药一起消失。前两条同义词（等价改名）干净，值得单独留下 |
| `feat/doctor-mode-landing` | ⏸ 桌面/官网，暂不理会 |
| `fix/scanner-timeout-fallback` | ❌ **永不合** —— 建立在已被证伪的前提上：看门狗判据在真实失败模式下无效，GMS 的下载界面占着前台，MedMe 根本不处于 `resumed` 状态 |
| `test/brute-force-journeys` | 📦 资产分支：重写后的集成测试 + 钉住已知缺陷的用例。修复落地后要把断言方向翻过来 |
| `test/data-layer-brute-force` | 📦 资产分支：50 条数据层测试，30 条 `#[ignore]` 标着已知缺陷编号 |

**已删**：`fix/disease-lane-normalization`（`git diff` 证实与 `ad433bc (#189)` **逐字节相同**，早就 squash 合过，留着只会制造冲突）、`fix/lab-row-recovery`（被 `-safe` 变体取代）。

其余全部已合。已合并的 worktree 已删（`target/` 单份 2–12 GB，见下方环境教训）。

**临时**：`demo/mobile-preview` —— 给产品看的合并分支，用完删

---

## 已定的产品决策（不要重新论证）

- **手动录入只做自测数值 + 笔记，不做手打化验值**。手打的「肌酐 95」和化验单读出来的在库里长得一样但没出处，破坏「原件永远可达」这条核心不变量。
- **自测值不与医院值并成一条线**。不同的测量。`GroupKey` 加第三分区。
- **家测参考区间用家测那套**（血压 135/85 vs 诊室 140/90）。查不到出处的项**不给区间、不给 flag**，显示裸值（体温即为此例）。
- **自测数据照常进医生二维码分享**，不加过滤。参考区间逐序列随 payload 走，查看器算不错结论；「家测」标注是正常队列里的改进，不是发版闸门。
- **趋势分类用检验大类，不用疾病泳道**。疾病泳道是给医生视图的，当浏览目录必然稀疏。
- **趋势默认只列非正常项**，隐藏条数始终写在开关旁边。
- **不做健康分、不做「本月指标向好」**这类综合评价。
- **血型不抽取也不让填** —— 刻意的安全设计，不是缺口。
- 内测阶段，**不需要向后兼容**（无老用户），可直接改 `Event` 枚举。

---

## 2026-08-05 真机测试轮（华为 Mate 9 / Android 8.0）

`.dev` 后缀已上（`applicationIdSuffix`），debug 与正式版 `com.medme.mobile` 并存，
真机验证不再有覆盖病历的风险。

**真机独有的三条发现**（模拟器 1080×2400 全部看不出来）：

| | 状态 |
|---|---|
| 手动录入无范围校验，实测存进 `138388` mmHg | ✅ 已修（#45） |
| 同意屏第 4 条 + 协议链接在 1920px 屏折线外，而同意按钮已可见 | ✅ 已修（#46） |
| 自测生命体征无 `panel`，手动录入全部产出掉进「其他」 | ✅ 已修 |
| 概览化验行：长名 + 长单位在 360dp 上被切，pill 掉行 | ✅ 已修（#48） |
| 最近归档卡片把同一件事说三遍（标题带日期 + 右列日期 + 副标题重复类型） | ✅ 已修（#48） |

**第二轮装机（两端都看）**：`emulator-5554` 411dp / 华为 MHA-L29 360dp。`LabLine` 改成
用 `TextPainter` **实测宽度**决定并排还是两行——华为窄屏上「估算肾小球滤过率 /
63 ml/min/1.73m2」走两行分支不再折断，模拟器宽屏上短名仍并排不白占一行。

> ⚠️ 这条修复的第一版**测试全绿而屏幕仍是坏的**。「不溢出」不等于「好看」：pill 被顶到
> 独占一行、数值折成 `63 ml/min/` + `1.73m2`，在不溢出的意义上全都合格。是装到机器上
> 看了才发现。**RenderFlex 溢出走 `FlutterError.reportError`，`tester.takeException()`
> 根本抓不到** —— 这类问题没有装机替代品。

**顺带修掉的规范级缺陷**：`MedMe.faint` #94A3B8 压白 **2.56:1**（需 4.5），用在同意屏
正文、二维码分享页「这张码就是钥匙」、认领页「云端那份会删除」等有法律/安全含义的
句子上，全仓 33 处 → `#5F7390`（白 4.84、bg 4.55）。

**端到端确认成立**：家测血压 138/88 判为「偏高」，用的是家测 135 而不是诊室 140 ——
这正是当初决定「自测数据给医生看」时争的那个点。

### 环境教训（不是代码问题，但会再犯）

1. **每个 worktree 独立编 Rust target 撑爆了启动盘**（54 GB，单份 2–12 GB 带 ONNX）。
   合并后应立即删除已合并 worktree 的 `target/`。**不要**改用共享 `CARGO_TARGET_DIR` ——
   cargo 对 target 加全局锁，并行会退化成串行。

   > ⚠️ **同一个坑第二次踩**（2026-08-05 晚，78 GB，可用空间掉到 1.2 GB）。上面这条只说了
   > 「合并后」，没说**并行派 agent 时要控制同时在编的份数** —— 一口气派 5 个 agent，每个
   > 又编一份，加起来就是 5×。清理只需删 `target/`（源码保留），42 GB 立刻回来。
   >
   > **可执行的规则**：同时在跑的 Rust worktree ≤ 3；纯 Dart 的任务明确告诉 agent
   > 「不需要跑 cargo」；每个 agent 干完自己删 `build/`、`target/`、`.dart_tool`。
2. **Flutter SDK 曾放在 `~/Documents`**，macOS TCC 中途拒绝访问导致全部构建/测试瘫痪。
   已移到 `~/flutter`。开发工具链不要放在 macOS 保护目录下。
3. 移动 SDK 后主工作区 `.dart_tool` 缓存失效，需 `flutter pub get` 重新解析。

### 待补

六项生理可能性范围（血压/心率/体温/体重/血糖）**全部未核实到指南出处** —— 两轮
检索都因 web search 配额耗尽未能进行。代码注释如实标注为「未核实到具体出处，取值
依据是生理学极限的保守外扩」。拒绝 138388 本身不需要指南背书，但这六个数字目前
没有指南支撑，应当补。

---

## 验证纪律：macOS 上验手机端 OCR 的陷阱

`packages/ocr` 有三个入口，**只有一个和手机端跑的是同一条路**：

| 函数 | 门控 | macOS 上实际用什么 |
|---|---|---|
| `recognize` | `#[cfg(target_os = "macos")]` 分支 | **Apple Vision** |
| `recognize_pdf` | 同上 | **Apple Vision** |
| `recognize_engine_layout` | `#[cfg(feature = "engine")]` | PP-OCRv5 ✓ |

**Vision 是按 `target_os` 门控的，不是按 feature。** 把依赖写成和手机端一样的
`default-features = false, features = ["engine"]` **也关不掉它** —— 在 macOS 上
编译就一定带进来。

所以唯一可靠的做法：**验手机端行为时只调 `recognize_engine_layout`**。

已经踩过一次：`labaudit/ocr-dump/*.pdf.txt` 是 Vision 的输出，被当成 PP-OCR 语料
支撑过一次判断（逗号小数点统计）。复查后那次结论没变，但那是运气 —— 两个引擎
错的地方不一样，误读模式、字符混淆都不可互推。

已加的防线：
- `scratchpad/valprobe` 遇到 `.pdf` **直接报错退出**，不再悄悄换引擎
- `openmed/labaudit` 的 `ScannedPdf` 分支注释里写明了引擎来源和不可推断性

要在 PDF 路径上得到手机端可比的数字，必须先把页面渲染成图，再走
`recognize_engine_layout`。

---

## 2026-08-05 晚 · 暴力测试轮（数据层 + 多用户旅程）

产品负责人的要求：「多弄几个家测试试 会不会有bug 暴力测试嘛」「你测得不够 要全方位
多角度 从不同用户角度 暴力测」。两路并行：Rust 数据层 + Flutter 多用户旅程。

### 挖出来的，按类

**「安静地给出错误临床值」四条 —— 已修（见 P0-49）。** 共性是伪造出来的数**来自参考
区间下限，所以永远生理上合理**，在趋势图上是一个干净的历史点，没有异常值可供察觉。

**「改了一半」两条 —— 已修。** 医生代拍丢弃 `pagesWithoutText`（当场漏页，患者走了
补不回来）；多页 TIFF 只识首页且报成功（issue #63 关早了）。

**UI 刷新两条 —— 修复中。** `setState(() => _future = ...)` 箭头体返回 `Future`：赋值
发生了，但断言在 `markNeedsBuild()` **之前**抛出，一次重建都没排上。**debug 必现、
release 正常**——而团队自己装的正是 `.dev` 的 debug 包。全仓只有应急卡和「看病带这个」
两处；概览/趋势/档案都为这件事专门写成语句块并留了注释。

### 没打穿的（这也是结论，别重复投入）

- panic / 算术溢出 / 指数级回溯 / 无界分配：**一条都没找到**。`cargo fuzz` 111 万次零 crash
- 「自测值绝不与医院值并线」：doc_type 大小写、拼错、缺失、载荷损坏各种角度**都没打穿**
- CAS 完全干净（0 字节、路径穿越文件名、哈希截断/大写/非 hex/多字节边界、投毒对象）
- HTML 导出无 XSS；二维码截断从不切半个字符串
- 手动录入的输入校验**一条没漏**（空值/半填/`0/0`/`999999`/20 位/负数/中文/连点 5 次）
- 笔记硬不变量守住：`<script>`、纯 emoji、3000+ 字四条恶意笔记**都没漏进**「复制全文给医生」
- 三档视口 × 三档字号（≤1.5×）零 RenderFlex 溢出

### 顺带发现的两个盲点

1. **fuzz target 从没覆盖产出临床数值的函数** —— 原先只 fuzz 了 `classify`/`guess_date`/
   `demographics`/`detect_language`，`extract_labs`/`meds`/`conditions`/`aggregate` 一次
   都没进过。已补。
2. **原有 4 个 integration_test 是死的，两层都坏**：跑都跑不起来（缺 `androidTest/`
   脚手架，patrol 与 `flutter test` 绑定互斥），且断言的是**旧的三 tab 架构**。已整批重写。

### 验证纪律的一条新补充

**「我没测到」不等于「没问题」。** 这一轮之前我对产品说过「没有 critical」，那句话当时
是错的——只是还没测到。后来挖出的四条全是最严重那一类。宁可说「这一块我没测」。


---

## 2026-08-06 · 1.6.0 发布

`main` 合入 113 个 commit(`bb7abcf`),四条通路全部走通:

| | 结果 |
|---|---|
| 公开下载 | `v1.6.0` build **56**。从 `releases/latest/download/MedMe-Android.apk` **实际下载验过**:正式签名 `CN=zyuan Guan`(与真机上现装的一致可覆盖升级)、**只有 `lib/arm64-v8a/`**。原来停在 7/30 的 1.3.6 |
| iOS | TestFlight 已上传,`UPLOAD SUCCEEDED with no errors` |
| 托管查看器 | 自动部署,带 CSP 修复 |
| 官网 | 首页截图 3 → 7 张,4+3 两排 |

### 三条新教训

**1. 看 tag 会误判发布状态。** Release 的 tag 只按版本号(`v1.6.0`)不含 build 号,所以 build 56 是**覆盖上传同一个 tag 的资产** —— 光看 Release 列表会以为「还是 55 那次」。**要看资产的 `updatedAt`,或者干脆从公开链接下下来 dump badging。**

**2. `head`/`tail` 截断过的输出不能用来证否。** 我跑 `gh secret list | head -10` 后断言 `POSTHOG_KEY` 不存在,据此推出「17 张 dashboard 全是空的」并开了一条任务。实际仓库有 **12 条** secret,它就在被截掉的那两行里,而且 7/29 就配好了。与本文件上一节那条「我没测到 ≠ 没问题」同源。

**3. 官网/APK 的版本必须对得上。** 医院名修复合进 main 时,公开下载还是 55(不含它)—— 而新截图里医院名是全的。**截图和用户下到的包不一致,比截图旧更糟**:它承诺了一个下载后得不到的东西。所以 bump 56 重出了一次。

### 发布时容易忘的两处非代码检查

- `deploy-viewer.yml` 只在 **push 到 `main` 且改动 `web/hosted-viewer/**`** 时触发。改了查看器却只推功能分支 = 不会部署。
- GitHub Pages 构建要 **8–10 分钟**,推完立刻 curl 拿到的是旧版。别把 CDN 延迟当成部署失败(我这次差点)。


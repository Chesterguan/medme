# 2026-09-18 · 病程档案模拟器 smoke(feat/advanced-a @ a910096,合并 UX Stage 1 之后)

iPhone 17 模拟器(iOS 26 / Xcode 26 工具链),本地后端 `services/api` @ 同一提交(`:9000`,`OTP_DRY_RUN`),包 = 仓库 `skills/sle/2026.09.1.json`。

| 步 | 结果 | 现象 |
|---|---|---|
| 1 拉包 | 过 | `GET /v1/skills/index.json` 与 `/v1/skills/sle/2026.09.1.json` 均 200,带 ETag;App 启动即拉(见 F1) |
| 2 模拟器 debug 包 | 过 | 增量编译 46 s;启动无异常 |
| 3 开启并看档案 | 过 | 示例数据(张建国)成员上,「趋势」首块出现入口卡「可以整理「系统性红斑狼疮」这个病」→ 开启 → 卡变「活动度 0/18」→ 档案页按包顺序出现:现行方案(药物带日期、激素/羟氯喹「还没读到」)、活动度、指标趋势(中文分组 + 没查到列表)、达标情况(DORIS/LLDAS 逐条「未知」并给理由与出处)、关闭按钮、出处(可折叠)、免责声明 |
| 4 断网重进 | 过 | 后端关掉、杀掉 App 重进 → 入口卡照常显示已开启状态(走缓存) |
| 5 缓存改一字节重进 | 过 | 卡退回「还没准备好 —— 联网之后点一下重试」,无崩溃、无半份;恢复文件后重进即恢复 |
| 6 出码 | 过 | 「给医生看 → 出码给医生看」上传 5.5 MB(51 份)成功出真码;分享格式对不带档案的路径逐字节不变由 Task 23 测试钉住;线上查看器尚未部署 `renderProfile`,会忽略 `profile` 键(老分享包与页面一致) |

**发现,已在同分支修(task-26-fix):**
- F1 启动时(同意书之前)就去拉包——趋势 tab 的卡在 tab 栈里就已挂载。公开、无标识,但同意前不该有任何网络请求 → 改成同意后才拉。
- F2 缓存文件坏了以后,重试只拉 index、不重下包(坏文件被当成「已安装」),永远自愈不了 → 「已安装」改为「能验签能加载」。

**顺带看到(不挡内测):** 示例数据里出现药名「片」(「…钙片 20mg qn」被拆成两段,parser,记 WORKLIST A32 ⑨);现行方案卡的目标注释是英文原文(出处逐字);入口卡是普通白卡蓝边,mockup 里的「档案本书脊」样式留 Stage 3。

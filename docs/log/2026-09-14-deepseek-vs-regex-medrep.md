# DeepSeek 抽取 vs 正则:MedRepBench 683 份实测(2026-09-14)

把「本地 OCR → 脱敏 → DeepSeek → 逐字校验」整条路放到 MedRepBench 683 份真实
中文化验单上,和现有正则路径同分母比,决定进阶版默认走哪一档。

## 怎么跑的

同一遍 PP-OCRv5 出两份产物:未脱敏文本进 ② 几何重建臂(= 生产今天走的
`recognize_engine_layout`),脱敏后文本/涂黑后图片进 ④ 臂送 DeepSeek。
`deid::assert_clean` 在送云之前挡一道,拦下就跳过、计数,不绕过。回来的 JSON
过 `deid::verify` 逐字子串校验:文本档不过就丢,图片档不过就标待核。

模型:**`deepseek-flash`**。计划里写的 `deepseek-v4-flash` /
`deepseek-v4-flash-vision-exp` 是旧名,`GET /models` 已经不列(只剩
`deepseek-flash` 和 `deepseek-v4-pro`),官方文档说旧名仍然受理、同一档价钱。
图片档不需要切块:小样本 5 份里只有 1 份 labs 为 0(该份文本档也只有 1 条),
没触发计划里「≥3 份为 0 就按行带纵切」的条件。

## 数字

| | ② 几何重建(现状) | ④ text | ④ image |
|---|---|---|---|
| 项目召回 | 65.8% (1678/2551) | 62.8% (1601/2551) | **79.8%** (2035/2551) |
| 值-名配对 | 58.4% (1490/2551) | 59.0% (1504/2551) | **77.1%** (1967/2551) |
| 参考区间归属 | 47.0% (998/2123) | 57.5% (1220/2123) | **77.2%** (1638/2123) |
| 错配率(值安错指标) | 12.5% (212/1702) | 6.6% (106/1611) | **3.6%** (74/2040) |
| 幻觉率 · 化验条目 | — | 19.8% (947/4785) | 0%(见下) |
| 幻觉率 · 全部校验对象 | — | 16.1% (1129/7017) | 0%(见下) |
| 待核 · 化验条目 | — | 0 | 35.6% (1538/4323) |
| 整份产出为空 | 1/683 | 98/683 | 58/683 |
| 每份耗时(OCR / LLM) | 3.7s / — | 3.7s / 23.6s | 3.8s / 8.7s |
| 每份 token | — | 5,819 | 2,766 |
| 每份成本(平峰/高峰) | — | $0.0033 / $0.0067 | $0.0012 / $0.0024 |

脱敏门拦下 0 份;OCR 失败 0 份;API 失败 0 份;图片档 7 份回的不是合法 JSON
(记为整份失败)。两臂合计 $3.09(平峰)/ $6.18(高峰)。

**图片档的 0% 幻觉率不是"它不编"**,是图片档按设计不丢弃、只打待核 —— 可比
的那个数是「待核 35.6%」。文本档反过来:0 待核,19.8% 直接丢。同一件事的两
种记法,别只看 0。

## 结论

- **门过了。** spec §6 三条线 58.7 / 49.7 / 24.3,两档都全部高于。
- **默认走图片档。** 三条指标各高 17–20 个点,错配率最低(3.6%,比现状的
  12.5% 低一大截 —— 这是最危险的一类错:值安到别的指标上),延迟只有文本档
  的三分之一,每份还便宜一半多。
- **文本档不做默认。** 它的项目召回(62.8%)比今天的 ② 几何重建(65.8%)还
  低;赢的只有参考区间归属和错配率。留作图片超限/隐私更保守时的退路。
- 值得记的:spec 里那组门槛(58.7 / 49.7 / 24.3)是更早一轮的正则基线,这次
  同批数据上 ② 跑出 65.8 / 58.4 / 47.0 —— PR #213 把生产从裸拼接换成布局重建
  之后,基线自己涨了。门槛该刷新成同批实测的 ② 值,别再拿老数字当及格线。
- 上游天花板没动:5068 条真值里 38.9% 的项目名过不了 `terminology::resolve`,
  10.7% 是定性结果。**换模型解决不了这两块**,词表该补还得补。

## 口径提醒

② 读的是未脱敏 OCR 文本,④ 读的是脱敏后的文本/涂黑后的图 —— ④ 是带着脱敏
代价比的,和产品里真实发生的一致;② 略占便宜。
